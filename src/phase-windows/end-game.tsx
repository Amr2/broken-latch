import React from 'react';
import ReactDOM from 'react-dom/client';
import EndGameWindow from '../components/stages/EndGame';
import '../index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EndGameWindow />
  </React.StrictMode>
);
